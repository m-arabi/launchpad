networks:
  net:
    driver: bridge
services:
  authelia:
    image: authelia/authelia
    container_name: authelia
    volumes:
      - ${AUTHELIA_CONFIG}:/config
    ports:
      - '127.0.0.1:9091:9091'
    restart: unless-stopped
    networks:
      - net
    environment:
      - TZ=${TZ}
    healthcheck:
      disable: false